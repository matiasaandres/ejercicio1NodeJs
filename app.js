// tabla del 5

multi = (tabla)=>{

    let number = 12;

    for (let index = 1; index <= number; index++) {
        

        console.log(` ${tabla} x ${index}` + ` = ${tabla*index}` );
    
        
    }

}

multi(355);

